import {List, Map} from 'immutable';
import {expect} from 'chai';

import reducer from '../src/reducer';

describe('reducer', () => {

	it('handles NEXT', () => {

	});

	it('handles ADD_TRACK', () => {

	});

	it('handles PAUSE', () => {

	});

	it('handles PLAY', () => {

	});
	
});